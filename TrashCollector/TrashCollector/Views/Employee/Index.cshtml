
@{
    ViewBag.Title = "Index";
}

<nav class="navbar navbar-expand-lg navbar-dark bg-primary" style="margin-bottom:7px;">
    <a class="navbar-brand" href="">David's Trash Collection</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="true" aria-label="Toggle navigation" style="">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse show" id="navbarColor01" style="">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index", "Customer")">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("LogOff", "Account")">logout</a>
            </li>
        </ul>
    </div>
</nav>
@{
    if (!Request.IsAuthenticated)
    {
        Response.Redirect(Url.Action("Index", "Home"));
    }
}

<nav class="navbar navbar-expand-lg navbar-dark bg-primary" style="margin-bottom:7px;">
    <a class="navbar-brand" href="">David's Trash Collection</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="true" aria-label="Toggle navigation" style="">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse show" id="navbarColor01" style="">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index", "Employee")">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("LogOff", "Account")">logout</a>
            </li>
        </ul>
    </div>
</nav>
<div class="row">
    <center>@ViewBag.Name</center>
</div>
<div class="row">
    <div class="col-sm-4">
        <a href='@Url.Action("Customers","Employee")'>
            <div class="card text-white bg-success mb-3" style="max-width: 20rem;">
                <div class="card-header">View Today's Customers</div>
                <div class="card-body">
                    <p class="card-text">See a list of customers on your route today.</p>
                </div>
            </div>
        </a>
    </div>
    <div class="col-sm-4">
        <a href='@Url.Action("Route","Employee")'>
            <div class="card text-white bg-success mb-3" style="max-width: 20rem;">
                <div class="card-header">View Route</div>
                <div class="card-body">
                    <p class="card-text">View today's Route</p>
                </div>
            </div>
        </a>
    </div>
</div>

